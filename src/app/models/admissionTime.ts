export default class AdmissionTime {
    public startTime: string;
    public endTime: string;

    // public hasIntersections = (at: AdmissionTime): boolean => {
    //     return this.endTime >= at.startTime || at.endTime >= this.startTime;
    // }
}
